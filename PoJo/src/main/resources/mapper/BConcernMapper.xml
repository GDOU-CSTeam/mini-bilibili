<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bili.pojo.mapper.user.BConcernMapper">

    <select id="selectConcernedByUserId" resultType="com.bili.pojo.vo.user.UserInfoShowVo">
        SELECT
            ui.id,
            ui.nickname,
            ui.avatar,
            ui.intro
        FROM
            b_user_info AS ui
                JOIN
            b_concern AS u ON u.concern_user_id = ui.id
        WHERE
            u.user_id = #{userId};
    </select>

    <select id="selectFansByUserId" resultType="com.bili.pojo.vo.user.UserInfoShowVo">
        SELECT
            ui.id,
            ui.nickname,
            ui.avatar,
            ui.intro
        FROM
            b_user_info AS ui
                JOIN
            b_concern AS u ON u.user_id = ui.id
        WHERE
            u.concern_user_id = #{userId};
    </select>
</mapper>